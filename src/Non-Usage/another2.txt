//대충 180도 왔다갔다하는 코드

/* #include <Servo.h>

Servo myServo;

void setup(){
    myServo.attach(2);
    myServo.write(90);
    delay(1000);
}

void loop(){
    for (int i = 0; i <= 180; i +=10) {
        myServo.write(i);
        delay(500);
    }
    delay(1000);
    for (int i = 180; i >= 0; i-=10) {
        myServo.write(i);
        delay(500);
    }
    delay(1000);
}
 */